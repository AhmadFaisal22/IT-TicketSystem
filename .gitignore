# OS
.DS_Store
Thumbs.db
desktop.ini

# Claude Code local settings (keep .claude/settings.json if you want shared hooks)
.claude/settings.local.json

# Docker env (has secrets — never commit)
.env.docker

# Temp / scratch files
page_source.html
*.tmp

# Editor
.vscode/
.idea/
*.suo
*.sln
